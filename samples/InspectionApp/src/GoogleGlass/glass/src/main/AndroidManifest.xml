<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.salesforce.glassdemo"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        >

        <activity
            android:name=".activities.SplashActivity"
            android:label="@string/app_name"
            >
            <!-- define voice trigger -->
            <meta-data
                android:name="com.google.android.glass.VoiceTrigger"
                android:resource="@xml/voice_trigger"
                />

            <!-- respond to voice trigger -->
            <intent-filter>
                <action android:name="com.google.android.glass.action.VOICE_TRIGGER"/>
            </intent-filter>

            <!-- define this activity to be the main activity. -->
            <!-- this isn't accessible within glass by default, but some methods can use this -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity"/>
        <activity android:name=".activities.SelectSiteActivity"/>
        <activity android:name=".activities.SelectInspectionActivity"/>
        <activity android:name=".activities.InspectionActivity"/>
        <activity android:name=".activities.HotwordActivity"/>
        <activity android:name=".activities.TiltActivity"/>
        <activity android:name=".activities.VideoActivity"/>
        <activity android:name=".activities.CameraActivity"/>
        <activity android:name=".activities.NoDocumentationActivity"/>
    </application>


</manifest>
